# Test listing bookmarks

# No folders initially
exec duckrow folders
stdout 'No bookmarks'
! stderr .

# Add some folders
mkdir project-a
mkdir project-b
exec duckrow add project-a
exec duckrow add project-b

# List should show both
exec duckrow folders
stdout 'Bookmarks \(2\)'
stdout 'project-a'
stdout 'project-b'
