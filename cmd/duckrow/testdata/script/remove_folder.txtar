# Test removing folders from the bookmarks list

# Setup: add a folder first
mkdir project-a
exec duckrow add project-a

# Remove it
exec duckrow remove-folder project-a
stdout 'Removed bookmark'
! stderr .

# Verify it's gone
exec duckrow folders
stdout 'No bookmarks'

# Removing a folder that isn't tracked should fail
! exec duckrow remove-folder /some/random/path
stderr 'not tracked'

# Remove requires an argument
! exec duckrow remove-folder
stderr 'accepts 1 arg'
