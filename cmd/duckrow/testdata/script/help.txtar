# Test help output lists all commands
exec duckrow --help
stdout 'manages AI agent skills'
stdout 'add'
stdout 'remove-folder'
stdout 'folders'
stdout 'status'
stdout 'skill'
stdout 'sync'
stdout 'registry'
stdout 'version'
! stderr .

# Test help for skill subcommand
exec duckrow skill --help
stdout 'install'
stdout 'uninstall'
stdout 'update'
stdout 'outdated'
stdout 'sync'

# Test help for skill install
exec duckrow skill install --help
stdout 'source-or-name'
stdout 'git URL'
stdout 'registry'

exec duckrow registry --help
stdout 'add'
stdout 'list'
stdout 'refresh'
stdout 'remove'
