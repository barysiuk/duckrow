# Test adding folders to the bookmarks list

# Add current directory (default)
exec duckrow bookmark add
stdout 'Bookmarked'
! stderr .

# Add a specific directory
mkdir myproject
exec duckrow bookmark add myproject
stdout 'Bookmarked'

# Adding the same folder again should fail
! exec duckrow bookmark add myproject
stderr 'already tracked'

# Adding a nonexistent folder should fail
! exec duckrow bookmark add /nonexistent/path
stderr 'does not exist'

# Adding a file (not a directory) should fail
! exec duckrow bookmark add somefile.txt
stderr 'not a directory'

-- somefile.txt --
not a directory
